# {{project_slug}}

{{project_description}}

## Team Tools & Domain Context

<!-- See team reference doc for CAVE ecosystem, caveclient, neuroglancer,
     task queue patterns, and connectomics-specific design decisions:
     [link TBD] -->

## Development Environment

```bash
# Install dependencies (creates virtual environment)
uv sync

# Add a new dependency
uv add <package>

# Linting / formatting
uv run ruff check src/
uv run ruff format src/
```

### Key Commands

| Command | Description |
|---------|-------------|
| `poe lab` | Launch Jupyter Lab |
{%- if template_type in ['analysis', 'library', 'task'] %}
| `poe profile` | Profile CPU with pyinstrument (HTML report) |
| `poe profile-all` | Profile CPU + memory with scalene |
{%- endif %}
{%- if template_type == 'library' %}
| `poe scratch-lab` | Jupyter Lab in the scratch/ directory |
| `poe test` | Run pytest with coverage |
| `poe doc-preview` | Preview documentation locally |
| `poe drybump patch/minor/major` | Dry-run version bump |
| `poe bump patch/minor/major` | Bump version, commit, tag and push |
{%- endif %}
{%- if template_type == 'task' %}
| `poe insert_tasks` | Insert tasks into the task queue |
| `poe launch_worker` | Launch a local worker process |
| `poe check_filequeue` | Check filequeue status |
| `poe reset_queue` | Reset the file queue |
| `poe make_scripts` | Generate Kubernetes scripts from config |
| `poe deploy_task` | Build scripts, launch cluster, deploy task |
| `poe delete_cluster` | Delete the Kubernetes cluster |
{%- endif %}

{% if template_type == 'oneoff' -%}
## Exploration

Notebook-first project. Keep explorations in root notebooks; move any reusable logic to `src/{{project_slug}}/`.

{% elif template_type == 'analysis' -%}
## Analysis Structure

Reusable code lives in `src/{{project_slug}}/`. Notebooks and scripts import from there.

### Profiling

- `poe profile <script.py>` — CPU profiling via pyinstrument (HTML report, good for iterating)
- `poe profile-all <script.py>` — CPU + memory via scalene (use when memory is suspect)

{% elif template_type == 'library' -%}
## Library Development

### Testing Strategy

Implement both integration tests and unit tests:

- **Integration tests**: Real-world workflows, end-to-end functionality
- **Unit tests**: Individual methods, edge cases, error conditions
- **Coverage target**: >90% line coverage, >85% branch coverage

```bash
poe test                                                       # full suite with coverage
uv run pytest tests/test_foo.py -v                             # single file
uv run pytest --cov={{project_slug}} --cov-report=html tests/  # HTML report
```

### Release Process

Releasing is two steps: bump the version, then publish it.

```bash
poe drybump patch   # preview what will change
poe bump patch      # bump version, commit, tag, push (also: minor, major)
gh release create vX.Y.Z --generate-notes   # publish to PyPI
```

`poe bump patch|minor|major`:

- refuses if the current version isn't on PyPI yet, since bumping would skip a
  version nobody can install (override with `BUMP_ALLOW_UNRELEASED=1`). In a new
  project the first bump is refused until the initial version is published.
- updates `pyproject.toml` and `src/{{project_slug}}/__init__.py`, runs `uv sync`, commits and tags `vX.Y.Z`
- pushes the commit and tag with `git push --follow-tags`
- prints `Bumped version: X → Y`

Publishing is a separate step: creating a GitHub Release (`gh release create vX.Y.Z --generate-notes`)
triggers `.github/workflows/publish.yml`, which checks that the tag matches the package version,
runs the tests, builds, and uploads to PyPI with trusted publishing (no API token).

One-time setup per project (the template can't do this for you):

1. On PyPI, add a trusted publisher (a "pending publisher" if the project doesn't exist yet):
   owner `{{github_user}}`, repository `{{project_slug}}`, workflow `publish.yml`, environment `pypi`.
2. On GitHub, create an environment named `pypi` in the repo settings.

### Scratch Dir

`poe scratch-lab` opens Jupyter Lab in `scratch/`, isolated from the main package environment.

{% elif template_type == 'task' -%}
## Task Queue Workflow

Workers pull tasks from a queue (filequeue for local dev, cloud queue for production).

1. Implement your task logic in `src/{{project_slug}}/task.py`
2. Configure task and cluster settings in `config/task.env` and `config/cluster.env`
3. Insert tasks: `poe insert_tasks`

### Kubernetes Deployment

```bash
# Step-by-step
poe make_scripts    # generate cluster/task scripts from config
poe launch_cluster  # create the GKE cluster
poe apply_task      # deploy the task to the cluster

# Or all-in-one
poe deploy_task

# Cleanup
poe delete_cluster
```

See `HOW_TO_USE_TASKS.md` for detailed setup instructions.

{% elif template_type == 'tabula-rasa' -%}
## Blank Slate

Add dependencies as needed with `uv add <package>`.

{% endif -%}
---

## Project Overview

<!-- Describe what this project does and why it exists -->

## Architecture & Key Files

<!-- List key files and how they fit together -->

## Data & External Dependencies

<!-- Document datasets, services, APIs, and file paths this project depends on -->

## Conventions & Patterns

<!-- Describe coding conventions, naming patterns, and design decisions -->

## Notes

<!-- Miscellaneous notes, gotchas, TODOs -->
